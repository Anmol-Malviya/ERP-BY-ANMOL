import { describe,expect,it } from 'vitest';
import { cleanFileName,validateFileRequest } from '../src/modules/files/policy.js';
describe('file upload policy',()=>{it('strips path traversal from client filenames',()=>{expect(cleanFileName('../../report.pdf')).toBe('report.pdf');expect(cleanFileName('..\\..\\photo.jpg')).toBe('photo.jpg')});it('rejects executable and SVG uploads',()=>{expect(()=>validateFileRequest('student-document','payload.exe','application/octet-stream',100)).toThrow();expect(()=>validateFileRequest('school-branding','logo.svg','image/svg+xml',100)).toThrow()});it('rejects MIME/extension mismatch',()=>{expect(()=>validateFileRequest('student-document','photo.pdf','image/jpeg',100)).toThrow(/extension/i)});it('enforces purpose-specific maximum size',()=>{expect(()=>validateFileRequest('school-branding','logo.png','image/png',6*1024*1024)).toThrow(/between/i);expect(validateFileRequest('school-branding','logo.png','image/png',1024).mime).toBe('image/png')})});
