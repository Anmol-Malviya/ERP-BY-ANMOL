import type { FastifyInstance } from 'fastify';import { PERMISSIONS } from '@erp/contracts';import { requirePermission } from '../../core/security.js';import { Student,Teacher,Staff } from '../people/models.js';import { Admission } from '../admissions/models.js';import { FeeInvoice } from '../fees/models.js';import { LeaveRequest } from '../leaves/models.js';
export async function analyticsRoutes(app:FastifyInstance){app.get('/api/analytics/overview',{preHandler:requirePermission(PERMISSIONS.ANALYTICS_READ)},async()=>{const[students,teachers,staff,admissions,pendingLeaves,feeAgg]=await Promise.all([Student.countDocuments(),Teacher.countDocuments(),Staff.countDocuments(),Admission.countDocuments({status:{$in:['APPLIED','UNDER_REVIEW','DOCUMENT_VERIFICATION']}}),LeaveRequest.countDocuments({status:'PENDING'}),FeeInvoice.aggregate([{$group:{_id:null,total:{$sum:'$total'},paid:{$sum:'$paid'},balance:{$sum:'$balance'}}}])]);return{success:true,data:{students,teachers,staff,pendingAdmissions:admissions,pendingLeaves,fees:feeAgg[0]||{total:0,paid:0,balance:0}}}});}
