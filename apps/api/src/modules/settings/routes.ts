import type{FastifyInstance}from'fastify';import{PERMISSIONS}from'@erp/contracts';import{requirePermission}from'../../core/security.js';import{SchoolSettings}from'./models.js';import{writeAudit}from'../../core/audit.js';
export async function settingsRoutes(app:FastifyInstance){app.get('/api/settings',{preHandler:requirePermission(PERMISSIONS.SCHOOL_MANAGE)},async()=>({success:true,data:(await SchoolSettings.findOne().lean())||{}}));app.patch('/api/settings',{preHandler:requirePermission(PERMISSIONS.SCHOOL_MANAGE)},async(request:any)=>{const before=await SchoolSettings.findOne().lean();const item=await SchoolSettings.findOneAndUpdate({},{$set:{...request.body,updatedAt:new Date()}},{new:true,upsert:true,setDefaultsOnInsert:true});await writeAudit({action:'settings.update',entity:'school-settings',before,after:item?.toObject(),ip:request.ip});return{success:true,data:item}});}
