import type{FastifyInstance}from'fastify';import{PERMISSIONS}from'@erp/contracts';import{registerCrud}from'../../core/crud.js';import{requirePermission}from'../../core/security.js';import{BiometricDevice,BiometricMapping,BiometricPunch}from'./models.js';
export async function biometricRoutes(app:FastifyInstance){await registerCrud(app,{prefix:'/api/biometric/devices',model:BiometricDevice,entity:'biometric-device',readPermission:PERMISSIONS.BIOMETRIC_READ,writePermission:PERMISSIONS.BIOMETRIC_WRITE,searchable:['name','deviceCode','location']});await registerCrud(app,{prefix:'/api/biometric/mappings',model:BiometricMapping,entity:'biometric-mapping',readPermission:PERMISSIONS.BIOMETRIC_READ,writePermission:PERMISSIONS.BIOMETRIC_WRITE});app.post('/api/biometric/punches',{preHandler:requirePermission(PERMISSIONS.BIOMETRIC_WRITE)},async(request:any)=>({success:true,data:await BiometricPunch.create(request.body)}));}
