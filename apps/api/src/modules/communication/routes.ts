import type { FastifyInstance } from 'fastify';import { PERMISSIONS } from '@erp/contracts';import { registerCrud } from '../../core/crud.js';import { Notice,Grievance,ChatMessage,KnowledgeResource } from './models.js';
export async function communicationRoutes(app:FastifyInstance){await registerCrud(app,{prefix:'/api/notices',model:Notice,entity:'notice',readPermission:PERMISSIONS.COMMUNICATION_READ,writePermission:PERMISSIONS.COMMUNICATION_WRITE,searchable:['title','body']});await registerCrud(app,{prefix:'/api/grievances',model:Grievance,entity:'grievance',readPermission:PERMISSIONS.COMMUNICATION_READ,writePermission:PERMISSIONS.COMMUNICATION_WRITE,searchable:['subject','description']});await registerCrud(app,{prefix:'/api/chat-messages',model:ChatMessage,entity:'chat-message',readPermission:PERMISSIONS.COMMUNICATION_READ,writePermission:PERMISSIONS.COMMUNICATION_WRITE,searchable:['body']});await registerCrud(app,{prefix:'/api/knowledge',model:KnowledgeResource,entity:'knowledge-resource',readPermission:PERMISSIONS.COMMUNICATION_READ,writePermission:PERMISSIONS.COMMUNICATION_WRITE,searchable:['title','description']});}
