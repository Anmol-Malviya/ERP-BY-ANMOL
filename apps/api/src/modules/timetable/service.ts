import { TimetableSlot } from './models.js';
export async function detectTimetableConflict(input:any,excludeId?:string){const base:any={sessionId:input.sessionId,day:input.day,period:input.period};if(excludeId)base._id={$ne:excludeId};const conflicts=await TimetableSlot.find({...base,$or:[{teacherId:input.teacherId},{sectionId:input.sectionId},...(input.room?[{room:input.room}]:[])]}).lean();return conflicts.map((x:any)=>({id:String(x._id),kind:String(x.teacherId)===String(input.teacherId)?'TEACHER':String(x.sectionId)===String(input.sectionId)?'SECTION':'ROOM',message:'Conflicting timetable slot'}));}
