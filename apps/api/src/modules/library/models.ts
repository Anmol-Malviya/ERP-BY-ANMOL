import { Schema,model } from 'mongoose';import { tenantPlugin } from '../../core/tenant-plugin.js';
const B=new Schema({title:{type:String,required:true},isbn:String,author:String,publisher:String,category:String,accessionNo:{type:String,required:true},status:{type:String,enum:['AVAILABLE','ISSUED','LOST','DAMAGED'],default:'AVAILABLE',index:true},shelf:String,createdAt:{type:Date,default:Date.now}},{versionKey:false});B.plugin(tenantPlugin);B.index({schoolId:1,accessionNo:1},{unique:true,partialFilterExpression:{deletedAt:null}});
const I=new Schema({bookId:{type:Schema.Types.ObjectId,ref:'LibraryBook',required:true,index:true},borrowerType:{type:String,enum:['STUDENT','TEACHER','STAFF'],required:true},borrowerId:{type:Schema.Types.ObjectId,required:true,index:true},issuedAt:{type:Date,default:Date.now},dueAt:{type:Date,required:true},returnedAt:Date,fine:Number,status:{type:String,enum:['ISSUED','RETURNED','OVERDUE','LOST'],default:'ISSUED'}},{versionKey:false});I.plugin(tenantPlugin);
export const LibraryBook=model('LibraryBook',B);export const BookIssue=model('BookIssue',I);
