import { Schema,model } from 'mongoose';import { tenantPlugin } from '../../core/tenant-plugin.js';
const S=new Schema({applicantType:{type:String,enum:['STUDENT','TEACHER','STAFF'],required:true},applicantId:{type:Schema.Types.ObjectId,required:true,index:true},fromDate:{type:Date,required:true},toDate:{type:Date,required:true},reason:{type:String,required:true},status:{type:String,enum:['PENDING','APPROVED','REJECTED','CANCELLED'],default:'PENDING',index:true},approverId:{type:Schema.Types.ObjectId,ref:'User'},decisionNote:String,decidedAt:Date,createdAt:{type:Date,default:Date.now}},{versionKey:false});S.plugin(tenantPlugin);export const LeaveRequest=model('LeaveRequest',S);
