import{Schema,model}from'mongoose';import{tenantPlugin}from'../../core/tenant-plugin.js';
const Form=new Schema({name:{type:String,required:true},slug:{type:String,required:true},purpose:{type:String,default:'GENERAL'},fields:[{key:String,label:String,type:String,required:Boolean,options:[String]}],status:{type:String,enum:['DRAFT','PUBLISHED','ARCHIVED'],default:'DRAFT'},public:{type:Boolean,default:false},createdAt:{type:Date,default:Date.now}},{versionKey:false});Form.plugin(tenantPlugin);Form.index({schoolId:1,slug:1},{unique:true,partialFilterExpression:{deletedAt:null}});
const Submission=new Schema({formId:{type:Schema.Types.ObjectId,ref:'CustomForm',required:true,index:true},values:Schema.Types.Mixed,status:{type:String,enum:['NEW','REVIEWED','CONVERTED','REJECTED'],default:'NEW'},createdAt:{type:Date,default:Date.now}},{versionKey:false});Submission.plugin(tenantPlugin);
export const CustomForm=model('CustomForm',Form);export const CustomFormSubmission=model('CustomFormSubmission',Submission);
