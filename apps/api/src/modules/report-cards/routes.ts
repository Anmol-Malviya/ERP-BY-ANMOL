import type { FastifyInstance } from 'fastify';import { PERMISSIONS } from '@erp/contracts';import { registerCrud } from '../../core/crud.js';import { requirePermission } from '../../core/security.js';import { Student } from '../people/models.js';import { Exam,Mark } from '../examinations/models.js';import { Attendance } from '../attendance/models.js';import { ReportCard,ReportTemplate } from './models.js';import { AppError } from '../../core/errors.js';
export async function reportCardRoutes(app:FastifyInstance){await registerCrud(app,{prefix:'/api/report-templates',model:ReportTemplate,entity:'report-template',readPermission:PERMISSIONS.REPORT_CARD_READ,writePermission:PERMISSIONS.REPORT_CARD_WRITE,searchable:['name']});app.post('/api/report-cards/generate',{preHandler:requirePermission(PERMISSIONS.REPORT_CARD_WRITE)},async(request:any)=>{const{studentId,examId,templateId}=request.body??{};const[student,exam,marks,attendance]=await Promise.all([Student.findById(studentId).lean(),Exam.findById(examId).lean(),Mark.find({studentId,examId}).lean(),Attendance.find({studentId}).lean()]);if(!student||!exam)throw new AppError(404,'NOT_FOUND','Student or exam not found');const obtained=marks.reduce((n:any,m:any)=>n+Number(m.total||0),0),max=(exam.subjectConfigs||[]).reduce((n:any,c:any)=>n+Number(c.maxMarks||0),0),percentage=max?Math.round((obtained/max)*10000)/100:0,present=attendance.filter((a:any)=>a.status==='PRESENT').length,attPct=attendance.length?Math.round((present/attendance.length)*10000)/100:0,grade=percentage>=90?'A+':percentage>=80?'A':percentage>=70?'B+':percentage>=60?'B':percentage>=50?'C':percentage>=33?'D':'F';const snapshot={student,exam,marks,attendance:{total:attendance.length,present,percentage:attPct},totals:{obtained,max,percentage,grade}};const card=await ReportCard.findOneAndUpdate({studentId,examId},{$set:{templateId,snapshot,percentage,grade,result:percentage>=33?'PASS':'FAIL'}},{new:true,upsert:true,setDefaultsOnInsert:true});return{success:true,data:card}});app.get('/api/report-cards/:studentId/:examId',{preHandler:requirePermission(PERMISSIONS.REPORT_CARD_READ)},async(request:any)=>({success:true,data:await ReportCard.findOne({studentId:request.params.studentId,examId:request.params.examId}).lean()}));}
