import{BrowserRouter,Navigate,Route,Routes}from'react-router-dom';import{AuthProvider,useAuth}from'./auth';import{Layout}from'./components/Layout';import Dashboard from'./pages/Dashboard';import Login from'./pages/Login';import ForgotPassword from'./pages/ForgotPassword';import ResetPassword from'./pages/ResetPassword';import ChangePassword from'./pages/ChangePassword';import ModulePage from'./pages/ModulePage';import Documents from'./pages/Documents';import BulkImport from'./pages/BulkImport';import ReportCards from'./pages/ReportCards';import PeopleWorkspace from'./pages/PeopleWorkspace';import Admissions from'./pages/Admissions';import Timetable from'./pages/Timetable';import Academics from'./pages/Academics';import{nav}from'./nav';
function Protected(){const{user}=useAuth();if(!user)return<Navigate to="/login" replace/>;if(user.mustChangePassword)return<Navigate to="/change-password" replace/>;return<Layout/>}
function PasswordGate(){const{user}=useAuth();return user?<ChangePassword/>:<Navigate to="/login" replace/>}
export default function App(){const dedicated=new Set(['/','/documents','/imports','/report-cards','/students','/teachers','/staff','/admissions','/academics','/timetable']),generic=nav.filter(([,to])=>!dedicated.has(to));return<BrowserRouter><AuthProvider><Routes><Route path="/login" element={<Login/>}/><Route path="/forgot-password" element={<ForgotPassword/>}/><Route path="/reset-password" element={<ResetPassword/>}/><Route path="/change-password" element={<PasswordGate/>}/><Route element={<Protected/>}><Route index element={<Dashboard/>}/><Route path="students" element={<PeopleWorkspace kind="students"/>}/><Route path="teachers" element={<PeopleWorkspace kind="teachers"/>}/><Route path="staff" element={<PeopleWorkspace kind="staff"/>}/><Route path="admissions" element={<Admissions/>}/><Route path="academics" element={<Academics/>}/><Route path="timetable" element={<Timetable/>}/><Route path="documents" element={<Documents/>}/><Route path="imports" element={<BulkImport/>}/><Route path="report-cards" element={<ReportCards/>}/>{generic.map(([,to])=><Route key={to} path={to.slice(1)} element={<ModulePage kind={to.slice(1)}/>}/>)}</Route><Route path="*" element={<Navigate to="/" replace/>}/></Routes></AuthProvider></BrowserRouter>}
