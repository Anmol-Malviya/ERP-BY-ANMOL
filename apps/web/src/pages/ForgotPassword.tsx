import{useState}from'react';import{Link}from'react-router-dom';import{ArrowLeft,MailCheck,ShieldCheck}from'lucide-react';import{api}from'../api';
export default function ForgotPassword(){const[schoolCode,setSchoolCode]=useState(''),[login,setLogin]=useState(''),[busy,setBusy]=useState(false),[message,setMessage]=useState(''),[error,setError]=useState('');const submit=async(e:any)=>{e.preventDefault();setBusy(true);setError('');try{const r:any=await api('/auth/forgot-password',{method:'POST',body:JSON.stringify({schoolCode,login})});setMessage(r.data.message)}catch(err:any){setError(err.message)}finally{setBusy(false)}};return<div className="login-page"><div className="login-brand"><div className="brand-mark large">A</div><b>ERP BY ANMOL</b></div><main className="login-card"><div className="login-kicker"><ShieldCheck size={16}/>Account recovery</div><h1>Reset your password</h1><p>Enter your school code and account email or username. For security, we always return the same response.</p>{message?<div className="auth-success"><MailCheck size={20}/><div><b>Check your inbox</b><span>{message}</span></div></div>:<form onSubmit={submit}><label>School code<input value={schoolCode} onChange={e=>setSchoolCode(e.target.value.toUpperCase())} required autoFocus/></label><label>Email or username<input value={login} onChange={e=>setLogin(e.target.value)} required/></label>{error&&<div className="form-error">{error}</div>}<button className="primary-btn full" disabled={busy}>{busy?'Sending…':'Send reset instructions'}</button></form>}<div className="auth-back"><Link to="/login"><ArrowLeft size={14}/>Back to sign in</Link></div></main></div>}
