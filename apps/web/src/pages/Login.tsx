import{useState}from'react';import{useNavigate}from'react-router-dom';import{useAuth}from'../auth';import{ArrowRight,ShieldCheck}from'lucide-react';
export default function Login(){const{login}=useAuth(),nav=useNavigate();const[schoolCode,setSchoolCode]=useState(''),[loginValue,setLoginValue]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);const submit=async(e:any)=>{e.preventDefault();setBusy(true);setError('');try{await login(schoolCode,loginValue,password);nav('/')}catch(err:any){setError(err.message)}finally{setBusy(false)}};return<div className="login-page"><div className="login-brand"><div className="brand-mark large">A</div><b>ERP BY ANMOL</b></div><main className="login-card"><div className="login-kicker"><ShieldCheck size={16}/>Secure school workspace</div><h1>Welcome back</h1><p>Sign in to continue to your school operations workspace.</p><form onSubmit={submit}><label>School code<input value={schoolCode} onChange={e=>setSchoolCode(e.target.value.toUpperCase())} placeholder="e.g. AVS2026" autoFocus/></label><label>Email or username<input value={loginValue} onChange={e=>setLoginValue(e.target.value)} placeholder="admin@school.edu"/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></label>{error&&<div className="form-error">{error}</div>}<button className="primary-btn full" disabled={busy}>{busy?'Signing in…':'Sign in'}<ArrowRight size={17}/></button></form><small className="secure-note">Protected by tenant isolation, rate limiting and role-based authorization.</small></main><footer>ERP BY ANMOL · Production V2</footer></div>}
