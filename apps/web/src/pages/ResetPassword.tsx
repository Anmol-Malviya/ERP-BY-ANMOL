import{useState}from'react';import{Link,useSearchParams}from'react-router-dom';import{ArrowLeft,CheckCircle2,ShieldCheck}from'lucide-react';import{api}from'../api';
export default function ResetPassword(){const[params]=useSearchParams();const[schoolCode,setSchoolCode]=useState(params.get('school')||''),[token,setToken]=useState(params.get('token')||''),[password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[busy,setBusy]=useState(false),[done,setDone]=useState(false),[error,setError]=useState('');const submit=async(e:any)=>{e.preventDefault();if(password!==confirm){setError('Passwords do not match');return}setBusy(true);setError('');try{await api('/auth/reset-password',{method:'POST',body:JSON.stringify({schoolCode,token,newPassword:password})});setDone(true)}catch(err:any){setError(err.message)}finally{setBusy(false)}};return<div className="login-page"><div className="login-brand"><div className="brand-mark large">A</div><b>ERP BY ANMOL</b></div><main className="login-card"><div className="login-kicker"><ShieldCheck size={16}/>Secure reset</div><h1>Create a new password</h1><p>The reset link is single-use and expires after 30 minutes.</p>{done?<div className="auth-success"><CheckCircle2 size={20}/><div><b>Password updated</b><span>Your previous sessions have been revoked.</span></div><Link className="primary-btn full" to="/login">Sign in</Link></div>:<form onSubmit={submit}><label>School code<input value={schoolCode} onChange={e=>setSchoolCode(e.target.value.toUpperCase())} required/></label><label>Reset token<input value={token} onChange={e=>setToken(e.target.value)} required/></label><label>New password<input type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/></label><label>Confirm password<input type="password" minLength={8} value={confirm} onChange={e=>setConfirm(e.target.value)} required/></label>{error&&<div className="form-error">{error}</div>}<button className="primary-btn full" disabled={busy}>{busy?'Updating…':'Update password'}</button></form>}<div className="auth-back"><Link to="/login"><ArrowLeft size={14}/>Back to sign in</Link></div></main></div>}
