import{useEffect,useMemo,useState}from'react';
import{BarChart3,CalendarCheck2,Check,Clock3,RefreshCw,Save,Search,UsersRound}from'lucide-react';
import{PERMISSIONS}from'@erp/contracts';import{api}from'../api';import{useAuth}from'../auth';import'../attendance.css';
type Student={_id:string;firstName:string;lastName?:string;admissionNo:string;rollNo?:string;status?:string;remark?:string};type Session={_id:string;name:string;isCurrent?:boolean};type Status='PRESENT'|'ABSENT'|'LATE'|'LEAVE';type FacultyStatus=Status|'HALF_DAY';
const statuses:Status[]=['PRESENT','ABSENT','LATE','LEAVE'];const today=()=>new Date().toISOString().slice(0,10);
export default function Attendance(){const{user}=useAuth(),canMark=!!user?.permissions?.includes(PERMISSIONS.ATTENDANCE_MARK),canStaffWrite=!!user?.permissions?.includes(PERMISSIONS.STAFF_WRITE),showFaculty=['SCHOOL_ADMIN','HR_MANAGER','TEACHER'].includes(user?.role||'');const[tab,setTab]=useState<'mark'|'records'|'faculty'>(canMark?'mark':'records');return<div className="page attendance-page"><div className="page-header"><div><div className="eyebrow">Attendance · Daily operations</div><h1>Attendance</h1><p>Role-scoped attendance with server-validated rosters, academic references, subject assignments and audit trails. Student and Parent records remain limited to linked profiles.</p></div></div><div className="attendance-tabs">{canMark&&<button className={tab==='mark'?'active':''} onClick={()=>setTab('mark')}><CalendarCheck2/>Mark attendance</button>}<button className={tab==='records'?'active':''} onClick={()=>setTab('records')}><BarChart3/>Student records</button>{showFaculty&&<button className={tab==='faculty'?'active':''} onClick={()=>setTab('faculty')}><UsersRound/>{canStaffWrite?'Faculty attendance':'My faculty attendance'}</button>}</div>{tab==='mark'&&canMark?<MarkAttendance/>:tab==='faculty'&&showFaculty?<FacultyAttendance canWrite={canStaffWrite}/>:<AttendanceRecords/>}</div>}
function MarkAttendance(){const[options,setOptions]=useState<any>(null),[mode,setMode]=useState<'DAILY'|'SUBJECT'>('SUBJECT'),[sessionId,setSessionId]=useState(''),[date,setDate]=useState(today()),[sectionId,setSectionId]=useState(''),[subjectId,setSubjectId]=useState(''),[period,setPeriod]=useState(1),[assignmentId,setAssignmentId]=useState(''),[roster,setRoster]=useState<Student[]>([]),[query,setQuery]=useState(''),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[error,setError]=useState(''),[message,setMessage]=useState('');
 useEffect(()=>{void(async()=>{setLoading(true);try{const r:any=await api('/attendance/marking-options');setOptions(r.data);const current=(r.data.sessions||[]).find((s:any)=>s.isCurrent)||r.data.sessions?.[0];if(current)setSessionId(String(current._id));if(r.data.mode==='TEACHER'){const first=r.data.assignments?.[0];if(first){setAssignmentId(String(first.id));setSectionId(String(first.sectionId));setSubjectId(String(first.subjectId))}else if(r.data.dailySections?.[0]){setMode('DAILY');setSectionId(String(r.data.dailySections[0].sectionId))}}}catch(e:any){setError(e.message)}finally{setLoading(false)}})()},[]);
 const teacherMode=options?.mode==='TEACHER',assignments=options?.assignments||[],adminSections=options?.sections||[],adminSubjects=options?.subjects||[],selectedAssignment=assignments.find((a:any)=>String(a.id)===assignmentId);useEffect(()=>{if(!teacherMode||mode!=='SUBJECT'||!selectedAssignment)return;setSectionId(String(selectedAssignment.sectionId));setSubjectId(String(selectedAssignment.subjectId))},[assignmentId,mode,teacherMode]);useEffect(()=>{if(mode==='DAILY')setSubjectId('')},[mode]);
 const loadRoster=async()=>{if(!sessionId||!sectionId)return;setLoading(true);setError('');setMessage('');try{const q=new URLSearchParams({sectionId,date,sessionId});if(mode==='SUBJECT'){if(!subjectId)throw new Error('Select a subject assignment');q.set('subjectId',subjectId);q.set('period',String(period))}const r:any=await api(`/attendance/roster?${q}`);setRoster((r.data.students||[]).map((student:any)=>({...student,status:student.status||'PRESENT',remark:student.remark||''}))}catch(e:any){setError(e.message);setRoster([])}finally{setLoading(false)}};useEffect(()=>{if(sessionId&&sectionId)void loadRoster()},[sessionId,sectionId,subjectId,period,date,mode]);
 const setAll=(status:Status)=>setRoster(rows=>rows.map(row=>({...row,status})));const setRow=(id:string,key:'status'|'remark',value:string)=>setRoster(rows=>rows.map(row=>row._id===id?{...row,[key]:value}:row));const save=async()=>{if(!roster.length)return;setSaving(true);setError('');setMessage('');try{await api('/attendance/mark',{method:'POST',body:JSON.stringify({date,sessionId,sectionId,subjectId:mode==='SUBJECT'?subjectId:undefined,period:mode==='SUBJECT'?period:undefined,records:roster.map(row=>({studentId:row._id,status:row.status||'PRESENT',remark:row.remark||undefined}))})});setMessage(`Saved attendance for ${roster.length} students.`);await loadRoster()}catch(e:any){setError(e.message)}finally{setSaving(false)}};
 const visible=useMemo(()=>{const q=query.toLowerCase().trim();return q?roster.filter(row=>`${row.firstName} ${row.lastName||''} ${row.admissionNo} ${row.rollNo||''}`.toLowerCase().includes(q)):roster},[roster,query]),counts=Object.fromEntries(statuses.map(status=>[status,roster.filter(row=>row.status===status).length]));
 return<><section className="attendance-controls"><div className="attendance-control-row"><label>Mode<select value={mode} onChange={e=>{const next=e.target.value as any;setMode(next);if(teacherMode&&next==='DAILY'){const daily=options?.dailySections?.[0];setSectionId(daily?String(daily.sectionId):'')}else if(teacherMode&&next==='SUBJECT'&&selectedAssignment){setSectionId(String(selectedAssignment.sectionId));setSubjectId(String(selectedAssignment.subjectId))}}}><option value="SUBJECT">Subject / period</option><option value="DAILY">Daily class attendance</option></select></label><label>Academic session<select value={sessionId} onChange={e=>setSessionId(e.target.value)}>{(options?.sessions||[]).map((s:Session)=><option key={s._id} value={s._id}>{s.name}{s.isCurrent?' · Current':''}</option>)}</select></label><label>Date<input type="date" max={today()} value={date} onChange={e=>setDate(e.target.value)}/></label>{teacherMode?mode==='SUBJECT'?<label className="wide-control">Teaching assignment<select value={assignmentId} onChange={e=>setAssignmentId(e.target.value)}><option value="">Select assignment</option>{assignments.map((a:any)=><option key={a.id} value={a.id}>{a.className} · {a.sectionName} · {a.subjectName}</option>)}</select></label>:<label>Class section<select value={sectionId} onChange={e=>setSectionId(e.target.value)}><option value="">Select class-teacher section</option>{(options?.dailySections||[]).map((s:any)=><option key={s.sectionId} value={s.sectionId}>{s.className} · {s.sectionName}</option>)}</select></label>:<><label>Section<select value={sectionId} onChange={e=>setSectionId(e.target.value)}><option value="">Select section</option>{adminSections.map((s:any)=><option key={s._id} value={s._id}>{refName(s.classId,options?.classes||[])} · {s.name}</option>)}</select></label>{mode==='SUBJECT'&&<label>Subject<select value={subjectId} onChange={e=>setSubjectId(e.target.value)}><option value="">Select subject</option>{adminSubjects.map((s:any)=><option key={s._id} value={s._id}>{s.name} · {s.code}</option>)}</select></label>}</>}{mode==='SUBJECT'&&<label>Period<input type="number" min="1" max="30" value={period} onChange={e=>setPeriod(Number(e.target.value))}/></label>}</div></section>{error&&<div className="form-error attendance-error">{error}</div>}{message&&<div className="attendance-success"><Check/>{message}</div>}<div className="attendance-summary">{statuses.map(status=><div key={status}><span className={`status-dot-lg ${status.toLowerCase()}`}/><div><small>{status}</small><b>{counts[status]||0}</b></div></div>)}</div><section className="roster-surface"><div className="roster-toolbar"><label><Search/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search roster…"/></label><div className="bulk-status">Mark all:{statuses.map(status=><button key={status} onClick={()=>setAll(status)}>{status[0]}</button>)}</div><button className="primary-btn" disabled={saving||!roster.length} onClick={()=>void save()}><Save size={14}/>{saving?'Saving…':'Save attendance'}</button></div><div className="roster-table"><table><thead><tr><th>Student</th><th>Roll</th><th>Status</th><th>Remark</th></tr></thead><tbody>{visible.map(row=><tr key={row._id}><td><div className="attendance-person"><span>{row.firstName?.[0]}</span><div><b>{row.firstName} {row.lastName||''}</b><small>{row.admissionNo}</small></div></div></td><td>{row.rollNo||'—'}</td><td><StatusButtons value={(row.status||'PRESENT') as Status} onChange={value=>setRow(row._id,'status',value)}/></td><td><input className="remark-input" value={row.remark||''} onChange={e=>setRow(row._id,'remark',e.target.value)} maxLength={300} placeholder="Optional"/></td></tr>)}</tbody></table></div><div className="roster-mobile">{visible.map(row=><div key={row._id}><div className="attendance-person"><span>{row.firstName?.[0]}</span><div><b>{row.firstName} {row.lastName||''}</b><small>{row.admissionNo} · Roll {row.rollNo||'—'}</small></div></div><StatusButtons value={(row.status||'PRESENT') as Status} onChange={value=>setRow(row._id,'status',value)}/><input className="remark-input" value={row.remark||''} onChange={e=>setRow(row._id,'remark',e.target.value)} maxLength={300} placeholder="Optional remark"/></div>)}</div>{loading&&<div className="attendance-loading">Loading roster…</div>}{!loading&&!roster.length&&<div className="attendance-empty"><UsersRound/><b>No roster loaded</b><span>Select a valid section/assignment and attendance context.</span></div>}</section></>}
function StatusButtons({value,onChange}:{value:Status;onChange:(value:Status)=>void}){return<div className="status-buttons">{statuses.map(status=><button key={status} className={`${status.toLowerCase()} ${value===status?'active':''}`} onClick={()=>onChange(status)} title={status}>{status[0]}</button>)}</div>}
function AttendanceRecords(){const[students,setStudents]=useState<Student[]>([]),[studentId,setStudentId]=useState(''),[summary,setSummary]=useState<any>(null),[rows,setRows]=useState<any[]>([]),[q,setQ]=useState(''),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{void(async()=>{try{const r:any=await api('/students?limit=200');setStudents(r.data||[]);if(r.data?.[0])setStudentId(String(r.data[0]._id))}catch(e:any){setError(e.message)}finally{setLoading(false)}})()},[]);useEffect(()=>{if(!studentId)return;void(async()=>{setLoading(true);setError('');try{const[s,r]:any[]=await Promise.all([api(`/attendance/summary/${studentId}`),api(`/attendance?studentId=${studentId}`)]);setSummary(s.data);setRows(r.data||[])}catch(e:any){setError(e.message)}finally{setLoading(false)}})()},[studentId]);const visible=useMemo(()=>{const query=q.toLowerCase();return query?rows.filter(row=>`${row.status} ${row.subjectId?.name||''} ${new Date(row.date).toLocaleDateString()}`.toLowerCase().includes(query)):rows},[rows,q]),breakdown=Object.fromEntries((summary?.breakdown||[]).map((x:any)=>[x._id,x.count]));return<><section className="record-controls"><label>Student<select value={studentId} onChange={e=>setStudentId(e.target.value)}>{students.map(student=><option key={student._id} value={student._id}>{student.firstName} {student.lastName||''} · {student.admissionNo}</option>)}</select></label><label className="record-search"><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Filter history…"/></label></section>{error&&<div className="form-error attendance-error">{error}</div>}<div className="attendance-kpis"><div><span>Attendance</span><b>{Number(summary?.percentage||0).toFixed(1)}%</b><small>Present + late weighted as attended</small></div><div><span>Present</span><b>{breakdown.PRESENT||0}</b><small>of {summary?.total||0} records</small></div><div><span>Absent</span><b>{breakdown.ABSENT||0}</b><small>Recorded absences</small></div><div><span>Leave / Late</span><b>{(breakdown.LEAVE||0)+(breakdown.LATE||0)}</b><small>Combined exceptions</small></div></div><section className="attendance-history"><div className="attendance-history-head"><div><small>RECENT RECORDS</small><h2>{visible.length} entries</h2></div><RefreshCw size={15}/></div>{visible.map((row:any)=><div className="attendance-history-row" key={row._id}><span className={`history-status ${String(row.status).toLowerCase()}`}>{row.status}</span><div><b>{row.subjectId?.name||'Daily attendance'}</b><small>{new Date(row.date).toLocaleDateString('en-IN',{day:'2-digit',month:'short',year:'numeric'})}{row.period?` · Period ${row.period}`:''}</small></div><p>{row.remark||'No remark'}</p></div>)}{!loading&&!visible.length&&<div className="attendance-empty"><BarChart3/><b>No attendance records</b><span>Records visible to this role will appear here.</span></div>}</section></>}
function FacultyAttendance({canWrite}:{canWrite:boolean}){const{user}=useAuth(),[date,setDate]=useState(today()),[people,setPeople]=useState<any[]>([]),[records,setRecords]=useState<any[]>([]),[history,setHistory]=useState<any[]>([]),[saving,setSaving]=useState(false),[loading,setLoading]=useState(true),[error,setError]=useState(''),[message,setMessage]=useState('');const load=async()=>{setLoading(true);setError('');try{const h:any=await api(`/faculty-attendance${canWrite?`?date=${date}`:''}`);setHistory(h.data||[]);if(canWrite){const[t,s]:any[]=await Promise.all([api('/teachers?limit=400'),api('/staff?limit=400')]);const all=[...(t.data||[]).map((x:any)=>({...x,personType:'TEACHER'})),...(s.data||[]).map((x:any)=>({...x,personType:'STAFF'}))];setPeople(all);const by=new Map((h.data||[]).map((x:any)=>[`${x.personType}:${x.personId}`,x]));setRecords(all.map((p:any)=>{const old:any=by.get(`${p.personType}:${p._id}`);return{personId:p._id,personType:p.personType,status:old?.status||'PRESENT',checkIn:old?.checkIn||'',checkOut:old?.checkOut||''}}))}}catch(e:any){setError(e.message)}finally{setLoading(false)}};useEffect(()=>{void load()},[date,canWrite]);const change=(id:string,type:string,key:string,value:string)=>setRecords(rows=>rows.map(row=>row.personId===id&&row.personType===type?{...row,[key]:value}:row));const save=async()=>{setSaving(true);setError('');setMessage('');try{await api('/faculty-attendance/mark',{method:'POST',body:JSON.stringify({date,records})});setMessage(`Saved ${records.length} faculty records.`);await load()}catch(e:any){setError(e.message)}finally{setSaving(false)}};return<>{error&&<div className="form-error attendance-error">{error}</div>}{message&&<div className="attendance-success"><Check/>{message}</div>}{canWrite?<section className="faculty-surface"><div className="faculty-toolbar"><label>Date<input type="date" max={today()} value={date} onChange={e=>setDate(e.target.value)}/></label><button className="primary-btn" disabled={saving||!records.length} onClick={()=>void save()}><Save size={14}/>{saving?'Saving…':'Save faculty attendance'}</button></div><div className="faculty-list">{people.map((person,index)=>{const row=records[index]||{};return<div key={`${person.personType}-${person._id}`}><div className="attendance-person"><span>{person.firstName?.[0]}</span><div><b>{person.firstName} {person.lastName||''}</b><small>{person.personType} · {person.employeeNo||person.department||'—'}</small></div></div><select value={row.status||'PRESENT'} onChange={e=>change(person._id,person.personType,'status',e.target.value)}>{(['PRESENT','ABSENT','LATE','LEAVE','HALF_DAY'] as FacultyStatus[]).map(status=><option key={status}>{status}</option>)}</select><input type="time" value={row.checkIn||''} onChange={e=>change(person._id,person.personType,'checkIn',e.target.value)}/><input type="time" value={row.checkOut||''} onChange={e=>change(person._id,person.personType,'checkOut',e.target.value)}/></div>)}</div>{loading&&<div className="attendance-loading">Loading faculty…</div>}</section>:<section className="attendance-history"><div className="attendance-history-head"><div><small>MY FACULTY ATTENDANCE</small><h2>{history.length} records</h2></div><Clock3/></div>{history.map((row:any)=><div className="attendance-history-row" key={row._id}><span className={`history-status ${String(row.status).toLowerCase()}`}>{row.status}</span><div><b>{new Date(row.date).toLocaleDateString('en-IN',{day:'2-digit',month:'short',year:'numeric'})}</b><small>{[row.checkIn&&`In ${row.checkIn}`,row.checkOut&&`Out ${row.checkOut}`].filter(Boolean).join(' · ')||'No time punches'}</small></div><p>{row.source||'MANUAL'}</p></div>)}</section>}</>}
function refName(value:any,items:any[]){const id=typeof value==='object'&&value?String(value._id):String(value||'');return items.find(item=>String(item._id)===id)?.name||id||'Class'}
