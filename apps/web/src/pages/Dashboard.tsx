import{useEffect,useState}from'react';import{Stat}from'../components/Page';import{CalendarDays,CheckCircle2,Clock3,ShieldCheck}from'lucide-react';import{api}from'../api';import{useAuth}from'../auth';
export default function Dashboard(){const[data,setData]=useState<any>(null),[error,setError]=useState('');const{user}=useAuth();useEffect(()=>{api('/dashboard/overview').then((r:any)=>setData(r.data)).catch((e:any)=>setError(e.message))},[]);const cards=data?.cards||[];return<><div className="page-header dashboard-head"><div><div className="eyebrow">{String(user?.role||'School').replaceAll('_',' ')}</div><h1>{data?.name?`Welcome, ${data.name}.`:'Good evening.'}</h1><p>Your workspace shows only role-relevant operational data and authorized modules.</p></div><button className="secondary-btn"><CalendarDays size={17}/>Academic calendar</button></div>{error&&<div className="form-error">{error}</div>}<div className="stats-grid">{cards.slice(0,4).map((c:any)=><Stat key={c.label} label={c.label} value={c.value} note={c.note}/>)}</div><div className="dashboard-grid"><section className="panel"><div className="panel-head"><div><span>Workspace</span><h2>Role-aware operations</h2></div></div><div className="pulse-grid"><div><span className="pulse-icon"><CheckCircle2/></span><b>Scoped</b><small>Data is tenant isolated</small></div><div><span className="pulse-icon"><ShieldCheck/></span><b>RBAC</b><small>Server permission checks</small></div><div><span className="pulse-icon"><Clock3/></span><b>15m</b><small>Short access sessions</small></div><div><span className="pulse-icon"><CalendarDays/></span><b>{data?.notices??data?.secondary?.notices??'—'}</b><small>Published notices</small></div></div></section><section className="panel"><div className="panel-head"><div><span>System integrity</span><h2>Production safeguards</h2></div></div><div className="activity-list">{[['Tenant isolation','Automatic school boundary on tenant models'],['Module entitlements','Disabled modules blocked by the API'],['Audit trail','Sensitive mutations retain actor context'],['Sessions','Short access token + rotating refresh']].map(([a,b])=><div className="activity" key={a}><span className="activity-dot"/><div><b>{a}</b><small>{b}</small></div></div>)}</div></section></div></>}
