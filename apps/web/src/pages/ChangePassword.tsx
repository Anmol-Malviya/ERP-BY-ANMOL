import{useState}from'react';import{useNavigate}from'react-router-dom';import{KeyRound,ShieldCheck}from'lucide-react';import{api}from'../api';import{useAuth}from'../auth';
export default function ChangePassword(){const{user,logout}=useAuth(),nav=useNavigate();const[currentPassword,setCurrent]=useState(''),[newPassword,setNext]=useState(''),[confirm,setConfirm]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);const submit=async(e:any)=>{e.preventDefault();if(newPassword!==confirm){setError('Passwords do not match');return}setBusy(true);setError('');try{await api('/account/change-password',{method:'POST',body:JSON.stringify({currentPassword,newPassword})});await logout();nav('/login',{replace:true})}catch(err:any){setError(err.message)}finally{setBusy(false)}};return<div className="login-page"><div className="login-brand"><div className="brand-mark large">A</div><b>ERP BY ANMOL</b></div><main className="login-card"><div className="login-kicker"><ShieldCheck size={16}/>First-login protection</div><h1>Change temporary password</h1><p>{user?.firstName}, your temporary password must be replaced before the ERP workspace can be accessed.</p><form onSubmit={submit}><label>Current password<input type="password" value={currentPassword} onChange={e=>setCurrent(e.target.value)} required autoFocus/></label><label>New password<input type="password" minLength={8} value={newPassword} onChange={e=>setNext(e.target.value)} required/></label><label>Confirm new password<input type="password" minLength={8} value={confirm} onChange={e=>setConfirm(e.target.value)} required/></label>{error&&<div className="form-error">{error}</div>}<button className="primary-btn full" disabled={busy}><KeyRound size={16}/>{busy?'Updating…':'Change password & sign in again'}</button></form><small className="secure-note">All existing refresh sessions are revoked after a password change.</small></main></div>}
