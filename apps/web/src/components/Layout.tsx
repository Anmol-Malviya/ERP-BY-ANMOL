import{useState}from'react';import{NavLink,Outlet}from'react-router-dom';import{Menu,Search,ChevronDown,LogOut,Command}from'lucide-react';import{nav}from'../nav';import{useAuth}from'../auth';
export function Layout(){const[open,setOpen]=useState(false);const{user,school,logout}=useAuth();return<div className="app-shell"><aside className={`sidebar ${open?'open':''}`}><div className="brand"><div className="brand-mark">A</div><div><strong>ERP BY ANMOL</strong><span>School Operations</span></div></div><nav className="nav-list">{nav.filter(([, , ,permission])=>!permission||user?.permissions?.includes(permission)).map(([label,to,Icon])=><NavLink key={to} to={to} end={to==='/'} onClick={()=>setOpen(false)} className={({isActive})=>`nav-item ${isActive?'active':''}`}><Icon size={18}/><span>{label}</span></NavLink>)}</nav><div className="sidebar-footer"><div className="school-chip"><span className="status-dot"/><div><b>{school?.name||'Your School'}</b><small>{school?.code||'Tenant'}</small></div></div></div></aside>{open&&<button className="scrim" onClick={()=>setOpen(false)}/>}<main className="main-area"><header className="topbar"><button className="icon-btn mobile-menu" onClick={()=>setOpen(true)}><Menu size={20}/></button><div className="search"><Search size={17}/><input placeholder="Search students, receipts, applications…"/><kbd><Command size={12}/> K</kbd></div><div className="top-actions"><button className="profile"><span className="avatar">{user?.firstName?.[0]||'A'}</span><span className="profile-copy"><b>{user?.firstName||'Admin'} {user?.lastName||''}</b><small>{(user?.role||'SCHOOL_ADMIN').replaceAll('_',' ')}</small></span><ChevronDown size={16}/></button><button className="icon-btn" onClick={()=>logout()}><LogOut size={18}/></button></div></header><section className="page"><Outlet/></section></main></div>}
